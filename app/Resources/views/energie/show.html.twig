{% extends 'base.html.twig' %}

{% block body %}
    <h1>Energie</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ energie.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ energie.type }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_energie_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_energie_edit', { 'id': energie.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
