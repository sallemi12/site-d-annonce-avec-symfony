{% extends 'base.html.twig' %}

{% block body %}
    <h3>Transmission</h3>
    <hr>
   <table class="table table-striped  table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transmission.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ transmission.type }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a class="btn ajouter" href="{{ path('admin_transmission_index') }}">Back to the list</a>
        </li>
        <li>
            <a  class="btn ajouter edit-button"  href="{{ path('admin_transmission_edit', { 'id': transmission.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
