{% extends 'base.html.twig' %}

{% block body %}
      <h3>Transmission creation</h3>
    <hr>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a class="btn ajouter " href="{{ path('admin_transmission_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
