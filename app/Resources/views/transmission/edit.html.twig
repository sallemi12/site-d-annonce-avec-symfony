{% extends 'base.html.twig' %}

{% block body %}
    <h3>Transmission edit</h3>
    <hr>
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn ajouter edit-button" type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a class="ajouter btn " href="{{ path('admin_transmission_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                        <input class="delete" type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
